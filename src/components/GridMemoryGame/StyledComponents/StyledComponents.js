import styled from '@emotion/styled'
import tw from 'tailwind.macro';
const CellDiv = styled.div `${tw`m-1 flex  justify-center items-center bg-blue-900 pointer-events-auto w-16 h-16`}`
const GameDiv = styled.div `${tw`flex border-2 border-solid border-black`}`
export { CellDiv, GameDiv };
