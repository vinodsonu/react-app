import styled from '@emotion/styled'
import tw from 'tailwind.macro';
const TryAgainButton = styled.button `${tw`bg-blue-500 hover:bg-blue-700 text-xl text-white font-bold py-3 px-4 mb-1 rounded `}`

export { TryAgainButton }
